# dependencies (โฟลเดอร์เก็บไลบรารี ห้ามเอาขึ้นเพราะขนาดใหญ่มาก)
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js (โฟลเดอร์แคชระบบของ Next.js)
/.next/
/out/

# production
/build

# debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# local env files (*** สำคัญที่สุด ห้ามเอาขึ้นเด็ดขาด เพราะเก็บคีย์ Supabase ของพี่ไว้ ***)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Vercel cache
.vercel

# TypeScript build info
*.tsbuildinfo